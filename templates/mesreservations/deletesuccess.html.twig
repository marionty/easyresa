{% extends 'base.html.twig' %}

{% block title %}Success{% endblock %}

{% block body %}
    <div class="container my-5">
        <div class="card shadow-lg">
            <div class="card-body">
                <h3 class="card-title text-center mb-4">Your reservation has been deleted!</h3>
                <div class="d-grid gap-2 col-2 mx-auto">
                    <a href="{{ path('app_home') }}" class="btn btn-warning">Return to home</a>
                </div>
            </div>
        </div>
    </div>
{% endblock %}